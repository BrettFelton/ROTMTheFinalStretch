@model IEnumerable<ROTM.mailing_list>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mailing_List_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mailing_List_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mailing_List_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mailing_List_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Mailing_List_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Mailing_List_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Mailing_List_ID })
        </td>
    </tr>
}

</table>
