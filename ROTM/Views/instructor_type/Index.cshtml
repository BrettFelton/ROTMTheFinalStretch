@model IEnumerable<ROTM.instructor_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instructor_Type_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instrutor_Type_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor_Type_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instrutor_Type_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Instructor_Type_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Instructor_Type_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Instructor_Type_ID })
        </td>
    </tr>
}

</table>
