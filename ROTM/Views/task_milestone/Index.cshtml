@model IEnumerable<ROTM.task_milestone>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Search: <input type="text" name="SearchString" placeholder="E.g. Show Promotions" value="@ViewData["currentFilter"]" />
            <input type="submit" value="Search" class="btn btn-default" /> |
            @Html.ActionLink("Show Full List", "Index", "task_milestone")
        </p>
    </div>
</form>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Task_Repetion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.milestone.Milestone_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.task.Task_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Task_Repetion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.milestone.Milestone_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.task.Task_Name)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
    </tr>
}

</table>
<div>
    @Html.ActionLink("Goto Employee Milestone", "Index", "employee_milestone")
</div>